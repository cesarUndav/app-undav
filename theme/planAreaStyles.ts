// theme/planAreaStyles.ts

import { StyleSheet } from 'react-native';

export const planAreaStyles = StyleSheet.create({
  box: {
    flex: 1,
    marginTop: 12,
    marginHorizontal: 16,
    borderRadius: 12,
    overflow: 'hidden',
  },
});